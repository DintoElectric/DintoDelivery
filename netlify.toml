[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: send every route to index.html so client nav works on reload.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
